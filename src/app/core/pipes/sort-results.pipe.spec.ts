import { SortResultsPipe } from './sort-results.pipe';

describe('SortResultsPipe', () => {
  it('create an instance', () => {
    const pipe = new SortResultsPipe();
    expect(pipe).toBeTruthy();
  });
});
